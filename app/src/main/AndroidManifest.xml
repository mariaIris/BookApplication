<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.bookapplication">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SearchBook"></activity>
        <activity android:name=".DeleteBook" />
        <activity android:name=".NewBookInflater" />
        <activity android:name=".LayoutCard" />
        <activity android:name=".InflaterPageView" />
        <activity android:name=".BookInflater" />
        <activity android:name=".CardView" />
        <activity android:name=".DragDrop" />
        <activity android:name=".RecycleView" />
        <activity android:name="adapters.AdapterSwipe" />
        <activity android:name=".ListBook" />
        <activity android:name=".ShowBook" />
        <activity android:name=".RegisterBook" />
        <activity android:name=".AutoComplete" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>